# .gitignore for a Node.js project using Cursor IDE

# Logs
# -------------------------------------------------------------------
logs
*.log
npm-debug.log*
yarn-error.log
lerna-debug.log*
pnpm-debug.log*

# Dependencies & Package Manager
# -------------------------------------------------------------------
# The node_modules folder contains all of your project's dependencies.
# It can be very large and should always be regenerated by running `npm install` or `yarn install`.
/node_modules

# Package manager cache files
.npm
.pnp.*

# IDE Specific: Cursor / VS Code
# -------------------------------------------------------------------
# These files store your personal workspace settings, local history, and AI server data.
# They should not be shared with other collaborators.
/.vscode/
/.cursor-server/
/.cursor-history/

# Environment Variables
# -------------------------------------------------------------------
# Never commit your secret keys, passwords, or API tokens!
# Store them in .env files, which are ignored here.
.env
.env.local
.env.*.local

# It IS a good practice to commit a template/example file.
# The '!' character below un-ignores a file that would have been ignored by a previous rule.
!*.env.example
!*.env.ci

# Build & Production Output
# -------------------------------------------------------------------
# These folders contain the compiled or bundled code that is generated from your source.
# They should be generated on the server or during a CI/CD build process, not committed.
/dist
/build
/.next/
/.nuxt/
/out/
/.output/
/.svelte-kit/

# Testing
# -------------------------------------------------------------------
# Test coverage reports and temporary test outputs.
/coverage
/.nyc_output

# Operating System Files
# -------------------------------------------------------------------
# Files generated by macOS, Windows, and Linux that are not part of the project.
.DS_Store
Thumbs.db
desktop.ini

# Miscellaneous Temporary Files
# -------------------------------------------------------------------
*.tmp
*.swp
*.swo